[project]
name = "kuya-bongs-car-selection-server"
version = "0.1.0"
description = "Serves data to client-facing applications"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]
